.{$pre}-{$container}{
    max-width: $container_width;
    padding-left: $container_gutter;
    padding-right: $container_gutter
    margin-left: auto;
    margin-right: auto;
    position: relative;
    z-index: 89;
    clearfix();
}